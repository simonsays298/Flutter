name: first_proj
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  http: ^0.12.2
  powers: ^0.1.0
  audioplayers: ^0.17.0
  file_picker: ^1.2.0
  assets_audio_player: ^2.0.13+1
  provider: ^4.3.2+2
  video_player: ^1.0.1
  audioplayer: ^0.5.2


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
  assets:
    - assets/translate_tts.mp3

